import React from 'react';
import NewHeader from './NewHeader';

function Klasses () {
	return (
		<React.Fragment>
			<NewHeader />
			<p>klasses page</p>
		</React.Fragment>
		
	);
}

export default Klasses;
